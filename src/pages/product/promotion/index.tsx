import React, { FC } from 'react';

interface PromotionProps {}

const Promotion: FC<PromotionProps> = (props) => {
  const {} = props;
  console.log('Promotion');
  return <div>Promotion</div>;
};

export default Promotion;
